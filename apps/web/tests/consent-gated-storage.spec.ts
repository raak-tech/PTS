/**
 * Sprint 1 persistence posture is covered elsewhere:
 * - `tests/sprint1-no-api-requests.spec.ts` — no browser requests to `/api/*` on UX_FLOW routes
 * - `tests/encrypted-reflections.spec.ts` — no support-storage / encryption controls on those routes
 *
 * This file is intentionally empty so the historical filename does not run duplicate E2E.
 */
export {};
