/** Prototype: 1 real hour advances the client program by 1 week. */
export const REAL_MS_PER_APP_WEEK = 60 * 60 * 1000;

export const APP_DAYS_PER_WEEK = 7;

export const PROGRAM_WEEK_COUNT = 6;

/** Refresh UI this often so day/week transitions show up during testing. */
export const PROGRAM_TIME_TICK_MS = 30_000;
